#!/bin/bash
#SBATCH --nodes=8                 # Number of nodes - all cores per node are allocated to the job
#SBATCH --time=00:15:00              # Wall clock time (HH:MM:SS) - once the job exceeds this time, the job will be terminated (default is 5 minutes)
#SBATCH --job-name=mpi              # Name of job
#SBATCH --output=mpi.out 

/home/knteran/mpi-ulfm-05022016/bin/mpirun --mca pml ob2 -am ft-enable-mpi  --mca btl openib,sm,self  --pernode  -n 8  ./mpi_ring 3
#/home/knteran/mpi-ulfm-1.0/bin/mpirun --mca pml ob1 -am ft-enable-mpi  --mca btl openib,sm,self  --pernode  -n 8  ./test.exe 2
#/home/knteran/mpi-ulfm-050115/bin/mpirun --mca pml ob1 -am ft-enable-mpi  --mca btl openib,sm,self  --bind-to-core  -n 256  ./test.exe 16

ls
